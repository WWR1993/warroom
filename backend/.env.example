# Example environment variables
DATABASE_URL=postgres://warroom:warroom@localhost:5432/warroom
POLL_INTERVAL=60
FRONTEND_ORIGIN=*
ADMIN_TOKEN=changeme
# FEEDS is now managed via the admin UI and DB; you can seed via docker-compose or add via the admin endpoints
